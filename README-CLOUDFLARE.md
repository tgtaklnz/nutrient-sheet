# Nutrient Lookup — Cloudflare Pages version

Static website. No Node.js, Render, package.json, or server is required.

GitHub repository layout:
- index.html
- app.js
- style.css
- foods.json
- _headers

Cloudflare Pages settings: leave Build command blank and use the repository root as the output directory.
